# DATABASE SERVER SETUP
- name: "Setup database server"
  hosts: database
  remote_user: ubuntu
  become: true
  gather_facts: no

  tasks:
    - name: "Wait for SSH to become available"
      wait_for_connection:
        connect_timeout: 10
        sleep: 5
        delay: 5
        timeout: 180

    - name: "Gather facts"
      setup:

    - name: "Update apt packages"
      apt:
        update_cache: yes

    - name: "Install required packages"
      apt:
        name: "{{ item }}"
        state: present
      loop:
        - apt-transport-https
        - ca-certificates
        - curl
        - software-properties-common

    - name: "Add Docker GPG key"
      apt_key:
        url: https://download.docker.com/linux/ubuntu/gpg
        state: present

    - name: "Add Docker repository"
      apt_repository:
        repo: "deb [arch=amd64] https://download.docker.com/linux/ubuntu {{ ansible_distribution_release }} stable"
        state: present

    - name: "Install Docker"
      apt:
        name: docker-ce
        state: present
        update_cache: yes

    - name: "Start and enable Docker service"
      systemd:
        name: docker
        state: started
        enabled: yes

    - name: "Add ubuntu user to docker group"
      user:
        name: ubuntu
        groups: docker
        append: yes

    - name: "Reset SSH connection to allow user changes to take effect"
      meta: reset_connection

    - name: "Install Docker Compose"
      get_url:
        url: https://github.com/docker/compose/releases/download/v2.20.2/docker-compose-Linux-x86_64
        dest: /usr/local/bin/docker-compose
        mode: '0755'

    - name: "Create app directory"
      file:
        path: /home/ubuntu/app
        state: directory
        owner: ubuntu
        group: ubuntu

    - name: "Copy docker-compose for database"
      copy:
        src: ../docker-compose-db.yml
        dest: /home/ubuntu/app/docker-compose.yml
        owner: ubuntu
        group: ubuntu

    - name: "Create .env file for database"
      copy:
        content: |
          DB_IMAGE={{ db_image }}
          DB_USER={{ db_user }}
          DB_PASSWORD={{ db_password }}
        dest: /home/ubuntu/app/.env
        owner: ubuntu
        group: ubuntu

    - name: "Install AWS CLI"
      apt:
        name: awscli
        state: present

    - name: "Log in to ECR"
      shell: "aws ecr get-login-password --region {{ aws_region }} | docker login --username AWS --password-stdin {{ ecr_registry }}"
      become: false

    - name: "Run docker-compose for database"
      community.docker.docker_compose_v2:
        project_src: /home/ubuntu/app
      become: false

# BACKEND SERVER SETUP
- name: "Setup backend server"
  hosts: backend
  remote_user: ubuntu
  become: true
  gather_facts: no

  tasks:
    - name: "Wait for SSH to become available"
      wait_for_connection:
        connect_timeout: 10
        sleep: 5
        delay: 5
        timeout: 180

    - name: "Gather facts"
      setup:

    - name: "Update apt packages"
      apt:
        update_cache: yes

    - name: "Install required packages"
      apt:
        name: "{{ item }}"
        state: present
      loop:
        - apt-transport-https
        - ca-certificates
        - curl
        - software-properties-common

    - name: "Add Docker GPG key"
      apt_key:
        url: https://download.docker.com/linux/ubuntu/gpg
        state: present

    - name: "Add Docker repository"
      apt_repository:
        repo: "deb [arch=amd64] https://download.docker.com/linux/ubuntu {{ ansible_distribution_release }} stable"
        state: present

    - name: "Install Docker"
      apt:
        name: docker-ce
        state: present
        update_cache: yes

    - name: "Start and enable Docker service"
      systemd:
        name: docker
        state: started
        enabled: yes

    - name: "Add ubuntu user to docker group"
      user:
        name: ubuntu
        groups: docker
        append: yes

    - name: "Reset SSH connection to allow user changes to take effect"
      meta: reset_connection

    - name: "Install Docker Compose"
      get_url:
        url: https://github.com/docker/compose/releases/download/v2.20.2/docker-compose-Linux-x86_64
        dest: /usr/local/bin/docker-compose
        mode: '0755'

    - name: "Create app directory"
      file:
        path: /home/ubuntu/app
        state: directory
        owner: ubuntu
        group: ubuntu

    - name: "Copy docker-compose for backend"
      copy:
        src: ../docker-compose-backend.yml
        dest: /home/ubuntu/app/docker-compose.yml
        owner: ubuntu
        group: ubuntu

    - name: "Create .env file for backend"
      copy:
        content: |
          BACKEND_IMAGE={{ backend_image }}
          DB_HOST={{ db_private_ip }}
          DB_USER={{ db_user }}
          DB_PASSWORD={{ db_password }}
        dest: /home/ubuntu/app/.env
        owner: ubuntu
        group: ubuntu

    - name: "Install AWS CLI"
      apt:
        name: awscli
        state: present

    - name: "Log in to ECR"
      shell: "aws ecr get-login-password --region {{ aws_region }} | docker login --username AWS --password-stdin {{ ecr_registry }}"
      become: false

    - name: "Wait for database to be ready"
      wait_for:
        host: "{{ db_private_ip }}"
        port: 5432
        timeout: 60

    - name: "Run docker-compose for backend"
      community.docker.docker_compose_v2:
        project_src: /home/ubuntu/app
      become: false

# FRONTEND SERVER SETUP
- name: "Setup frontend server"
  hosts: frontend
  remote_user: ubuntu
  become: true
  gather_facts: no

  tasks:
    - name: "Wait for SSH to become available"
      wait_for_connection:
        connect_timeout: 10
        sleep: 5
        delay: 5
        timeout: 300

    - name: "Gather facts"
      setup:

    - name: "Update apt packages"
      apt:
        update_cache: yes

    - name: "Install required packages"
      apt:
        name: "{{ item }}"
        state: present
      loop:
        - apt-transport-https
        - ca-certificates
        - curl
        - software-properties-common

    - name: "Add Docker GPG key"
      apt_key:
        url: https://download.docker.com/linux/ubuntu/gpg
        state: present

    - name: "Add Docker repository"
      apt_repository:
        repo: "deb [arch=amd64] https://download.docker.com/linux/ubuntu {{ ansible_distribution_release }} stable"
        state: present

    - name: "Install Docker"
      apt:
        name: docker-ce
        state: present
        update_cache: yes

    - name: "Start and enable Docker service"
      systemd:
        name: docker
        state: started
        enabled: yes

    - name: "Add ubuntu user to docker group"
      user:
        name: ubuntu
        groups: docker
        append: yes

    - name: "Reset SSH connection to allow user changes to take effect"
      meta: reset_connection

    - name: "Install Docker Compose"
      get_url:
        url: https://github.com/docker/compose/releases/download/v2.20.2/docker-compose-Linux-x86_64
        dest: /usr/local/bin/docker-compose
        mode: '0755'

    - name: "Create app directory"
      file:
        path: /home/ubuntu/app
        state: directory
        owner: ubuntu
        group: ubuntu

    - name: "Copy docker-compose for frontend"
      copy:
        src: ../docker-compose-frontend.yml
        dest: /home/ubuntu/app/docker-compose.yml
        owner: ubuntu
        group: ubuntu

    - name: "Create .env file for frontend"
      copy:
        content: |
          FRONTEND_IMAGE={{ frontend_image }}
          BACKEND_HOST={{ backend_public_ip }}
        dest: /home/ubuntu/app/.env
        owner: ubuntu
        group: ubuntu

    - name: "Install AWS CLI"
      apt:
        name: awscli
        state: present

    - name: "Log in to ECR"
      shell: "aws ecr get-login-password --region {{ aws_region }} | docker login --username AWS --password-stdin {{ ecr_registry }}"
      become: false

    - name: "Wait for backend to be ready"
      wait_for:
        host: "{{ backend_public_ip }}"
        port: 8080
        timeout: 60

    - name: "Run docker-compose for frontend"
      community.docker.docker_compose_v2:
        project_src: /home/ubuntu/app
      become: false