node_modules
.env
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json

# Generated fresh by the CI pipeline (and previously deploy.sh) on every run.
ansible/inventory.yml

# Provider locks are safe and should be committed for reproducible installs.
!.terraform.lock.hcl
