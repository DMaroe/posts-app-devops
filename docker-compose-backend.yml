services:
  backend:
    image: ${BACKEND_IMAGE}
    container_name: backend-service
    environment:
      PORT: 8080
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_HOST: ${DB_HOST}
    ports:
      - "8080:8080"
